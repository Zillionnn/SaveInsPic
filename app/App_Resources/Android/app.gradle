// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.saveInsPic"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
